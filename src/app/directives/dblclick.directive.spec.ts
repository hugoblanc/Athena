import { DblclickDirective } from './dblclick.directive';

describe('DblclickDirective', () => {
  it('should create an instance', () => {
    const directive = new DblclickDirective();
    expect(directive).toBeTruthy();
  });
});
